{
  "name": "site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:preview": "pnpm build && python3 -m http.server -d ./dist",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.19.3",
    "jflb-live": "workspace:*",
    "video-trainer": "workspace:*"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.7.0",
    "typescript": "^5.2.2",
    "vite": "^5.0.12"
  }
}
